<div class="container">
	<h1>Photos SocialNetwork</h1>
	<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
    		
    	</div>

    	<div class="collapse navbar-collapse" id="main-navigation-bar">
			<ul class="nav navbar-nav">
				{{#if session.isAuthenticated}}
	        		{{#link-to 'dashboard' tagName="li"}}<a href="">Dashboard</a>{{/link-to}}
	        		{{#link-to 'profile' tagName="li"}}<a href="">Profile</a>{{/link-to}}

	        	{{else}}
	        		{{#link-to 'index' tagName="li"}}<a href="">Home</a>{{/link-to}}
	        		{{#link-to 'signin' tagName="li"}}<a href="">Sign In</a>{{/link-to}}
	        		{{#link-to 'signup' tagName="li"}}<a href="">Sign Up</a>{{/link-to}}
	        	{{/if}}
	        </ul>
	        {{#if session.isAuthenticated}}
	        	<p class="navbar-text navbar-right">Signed in as {{session.currentUser.email}} <a href="/" {{action "onSignOut"}}>Logout</a>
</p>
	        {{/if}}
	     </div>
  	</div>
</nav>
	{{outlet}}
</div>